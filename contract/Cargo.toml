[package]
name = "solen-dex"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
solen-contract-sdk = { path = "../../solen/crates/solen-contract-sdk" }

[profile.release]
opt-level = "z"
lto = true
strip = true
